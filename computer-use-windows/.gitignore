__pycache__/
*.pyc
# Runtime demo output (recreated on submit)
data/*.json
# Never commit private keys created by the AWS deploy script
aws-deploy/*.pem
# Per-deploy artifacts (instance-specific, may contain secrets)
aws-deploy/*.rdp
aws-deploy/ssm_*.json
